WebStorage
- local / session

- localStorage : key : value, 문자열로 저장, json

- 사용
window.localStorage.
- setItem(k : v)
- getItem(k)
- removeItem()
- clear()
- length
- key(idx) : 로컬스토리지의 인덱스 번호로 키 값을 출력해주는 메소드